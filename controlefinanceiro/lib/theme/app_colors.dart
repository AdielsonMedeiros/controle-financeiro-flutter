import 'package:flutter/material.dart';

class AppColors {
  // Light Theme - Paleta azul + verde + roxo
  static const lightPrimary = Color(0xFF2563EB); // Azul royal
  static const lightSecondary = Color(0xFF10B981); // Verde esmeralda
  static const lightAccent = Color(0xFF8B5CF6); // Roxo
  static const lightBackground = Color(0xFFF8FAFC);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightError = Color(0xFFEF4444);
  
  // Dark Theme - Paleta brilhante
  static const darkPrimary = Color(0xFF60A5FA); // Azul claro
  static const darkSecondary = Color(0xFF34D399); // Verde claro
  static const darkAccent = Color(0xFFA78BFA); // Roxo claro
  static const darkBackground = Color(0xFF0F172A);
  static const darkSurface = Color(0xFF1E293B);
  static const darkError = Color(0xFFF87171);
  
  // Gradientes
  static const lightGradient = [Color(0xFFF0F9FF), Color(0xFFF0FDF4)];
  static const darkGradient = [Color(0xFF1E293B), Color(0xFF064E3B)];
  
  // Categorias
  static const income = Color(0xFF10B981);
  static const expense = Color(0xFFF43F5E);
  
  static const categoryColors = [
    Color(0xFF3B82F6), // Azul
    Color(0xFF8B5CF6), // Roxo
    Color(0xFF10B981), // Verde
    Color(0xFF06B6D4), // Ciano
    Color(0xFFF59E0B), // Laranja
    Color(0xFFEF4444), // Vermelho
    Color(0xFFEC4899), // Rosa
    Color(0xFF14B8A6), // Teal
  ];
}
